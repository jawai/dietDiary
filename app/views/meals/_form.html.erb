<%= form_with(model: meal, local: true) do |form| %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :meal_name %>
  </div>

  <div class="field">
    <%= form.label :meal_items %>
    <%= form.text_area :meal_items, id: :meal_meal_items %>
  </div>

  <div class="field">
    <%= form.label :calories %>
    <%= form.number_field :calories, id: :meal_calories %>
  </div>

  <div class="field">
    <%= form.label :mood %>
    <%= form.text_area :mood, id: :meal_mood %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :meal_start_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
